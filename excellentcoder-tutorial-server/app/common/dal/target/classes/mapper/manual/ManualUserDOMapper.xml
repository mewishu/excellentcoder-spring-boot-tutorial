<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.excellentcoder.tutorial.common.dal.mapper.manual.ManualUserDOMapper">

  <resultMap id="BaseResultMap" type="org.excellentcoder.tutorial.common.dal.dataobject.UserDO">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="email" jdbcType="VARCHAR" property="email" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="gmt_create" jdbcType="TIMESTAMP" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="TIMESTAMP" property="gmtModified" />
  </resultMap>


  <!-- 多对多:
    一、通过collection实现: 对应于注解中的@Many
    在ResultMap中进行嵌套结果集，然后在对应的sql中通过join实现

   -->
  <resultMap id="UserResultMap" type="org.excellentcoder.tutorial.common.dal.dataobject.UserDO" extends="BaseResultMap">
    <!-- 关联引用: RoleResultMap -->
    <collection property="roles" ofType="org.excellentcoder.tutorial.common.dal.dataobject.RoleDO" javaType="java.util.Set">
      <id column="r_id" jdbcType="BIGINT" property="id" />
      <result column="r_name" jdbcType="VARCHAR" property="name" />
    </collection>
  </resultMap>


  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <!-- 根据user表信息动态查询用户及对应的角色信息 -->
  <select id="selectByExample" parameterType="org.excellentcoder.tutorial.common.dal.dataobject.UserDOExample" resultMap="UserResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    'false' as QUERYID,
      u.id, u.name, u.username, u.email, u.password, u.gmt_create, u.gmt_modified,
      r.id as r_id,
      r.name as r_name
    from users u
      left join user_roles ur on u.id = ur.user_id
      left join roles r on ur.role_id = r.id
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>

  <!-- 根据user表中的id查询用户及对应的角色信息 -->
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="UserResultMap">
    select 
      u.id, u.name, u.username, u.email, u.password, u.gmt_create, u.gmt_modified,
      r.id as r_id,
      r.name as r_name
    from users u
      left join user_roles ur on u.id = ur.user_id
      left join roles r on ur.role_id = r.id
    where u.id = #{id,jdbcType=BIGINT}
  </select>

  <!-- 删除用户和角色之间的关系信息 -->
  <delete id="deleteUserRoleLink" parameterType="org.excellentcoder.tutorial.common.dal.dataobject.UserRolesLinkDO">
    delete from user_roles
    <trim prefix="WHERE" prefixOverrides="AND |OR ">
      <if test="user_id != 0">
        user_id = #{userDO.id,jdbcType=BIGINT}
      </if>
      <if test="role_id != 0">
        and role_id = #{roleDO.id,jdbcType=BIGINT}
      </if>
    </trim>
  </delete>

  <!-- 插入用户和角色之间的关系信息 -->
  <insert id="insertUserRoleLink" parameterType="org.excellentcoder.tutorial.common.dal.dataobject.UserRolesLinkDO">
    insert into user_roles (user_id, role_id)
    values (#{userDO.id,jdbcType=BIGINT}, #{roleDO.id,jdbcType=BIGINT})
  </insert>

</mapper>